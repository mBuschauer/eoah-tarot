import React, { useState, useEffect } from 'react';

export function AllCards({ selectedDeck }){
    console.log(selectedDeck.data)
    // this should create a table of all the cards and on click, it shows information about the card.
    return (<></>);
}